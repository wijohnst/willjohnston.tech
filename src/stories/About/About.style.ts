import styled from 'styled-components';

export const SemanticElement = styled.section`
  h1 {
    margin: 0.25rem;
  }

  p {
    margin: 0 0.25rem 0 0;
  }

  .content-wrapper {
  }

  .about-pic-wrapper {
    margin: 0.25rem;
    float: left;
  }

  .about-text-wrapper {
  }
`;
